[http]
	sslcapath = .
	sslcainfo = gitserver.crt
[user]
	email = kylewlacy@me.com
	name = Kyle Lacy
[mergetool]
	keepBackup = true
[alias]
	h = help
	s = status
  ss = "status -uno"
	co = checkout
	aa = "add --all"
  au = "add -u"
  aas = "!git add --all && git status"
  aus = "!git add --all && git status"
  df = "!git --work-tree=$HOME --git-dir=$HOME/.dotfiles.git"
  fa = fetch --all
	man = help
	am = commit --amend
  undo = reset --hard
  history = reflog show HEAD
  unstage = rm --cached
  cld = "!for i in `git status | grep deleted | awk '{print $3}'`; do git rm $i; done"
	c = commit
	ca = commit -a
	cm = commit -m
	cc = commit -a -m 
	
	d = diff
	dc = diff --check
	dl = diff HEAD~1
	
	l = "!source ~/.gitscripts/log.sh"
	ls = !git l --stat --color
  ll = "!source ~/.gitscripts/log-extended.sh"
	lf = "!source ~/.gitscripts/log-details.sh"
	ld = !git lf -n1
	lt = "!source ~/.gitscripts/log-tree.sh"
		
	cl = clone
	clg = "!source ~/.gitscripts/clone-github.sh"
[core]
	editor = vim
	excludesfile = /Users/kyle/.gitignore
[color]
	ui = true
; [diff]
; 	tool = vimdiff
